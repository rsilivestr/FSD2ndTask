-
  var dropdownDefaults = {
    type: 'single',
    placeholder: 'Сколько гостей',
    listItems: [
      {
        name: 'взрослые',
        nameSingle: 'взрослый',
        nameFew: 'взрослыx',
        nameMany: 'взрослыx',
        min: 1,
        max: 10,
        value: 0,
      },
      {
        name: 'дети',
        nameSingle: 'ребенок',
        nameFew: 'ребенка',
        nameMany: 'детей',
        min: 0,
        max: 10,
        value: 0,
      },
      {
        name: 'младенцы',
        nameSingle: 'младенец',
        nameFew: 'младенца',
        nameMany: 'младенцев',
        min: 0,
        max: 10,
        value: 0,
      },
    ],
  };

  var dropdownMultiDefaults = {
    type: 'multi',
    listItems: [
      {
        name: 'спальни',
        nameSingle: 'спальня',
        nameFew: 'спальни',
        nameMany: 'спален',
        min: 1,
        max: 3,
        value: 2,
      },
      {
        name: 'кровати',
        nameSingle: 'кровать',
        nameFew: 'кровати',
        nameMany: 'кроватей',
        min: 1,
        max: 6,
        value: 2,
      },
      {
        name: 'ванные комнаты',
        nameSingle: 'ванная комната',
        nameFew: 'ванных комнаты',
        nameMany: 'ванных комнат',
        min: 0,
        max: 2,
        value: 0,
      },
    ],
  };

mixin dropdown(options = {}, forceOpen = false)
  -
    const type = options.type || dropdownDefaults.type;
    const placeholder = options.placeholder || dropdownDefaults.placeholder;
    const listItems = options.listItems || dropdownDefaults.listItems;

  .dropdown(class = forceOpen ? 'dropdown--force-open' : '')&attributes(attributes)
    input(type='text' readonly placeholder = placeholder).dropdown__display.input-box__input
    .dropdown__contents(class = forceOpen ? 'dropdown__contents--force-visible' : '')
      each item in listItems
        .dropdown__option(
          data-min= item.min,
          data-max= item.max,
          data-single = item.nameSingle,
          data-few = item.nameFew,
          data-many = item.nameMany
        )
          span.dropdown__option-name= item.name
          button.dropdown__option-control.dropdown__option-control--subtract.dropdown__option-control--disabled -
          input.dropdown__option-value(value= item.value ? item.value : item.min)
          button.dropdown__option-control.dropdown__option-control--add +
      if (type === 'single')
        button.button.button--style_text.button--size_auto.dropdown__btn.dropdown__btn--clear.hidden очистить
        button.button.button--style_text.button--size_auto.dropdown__btn.dropdown__btn--apply применить
