mixin dateDropdown(options = {})
  -
    var type = options.type || 'single';
    var placeholder = options.placeholder || '';
    var labelStart = options.labelStart || 'Прибытие';
    var labelEnd = options.labelEnd || 'Выезд';

  if type === 'single'
    .date-dropdown.date-dropdown--range&attributes(attributes)
      input(type='text' readonly placeholder = placeholder).date-dropdown__display.input-box__input.datepicker-range

  else if type === 'double'
    .datepicker-double&attributes(attributes)
      +inputBox(labelStart, 'xs').mr5
        .date-dropdown
          input(type='text' readonly placeholder = placeholder).date-dropdown__display.input-box__input.date-dropdown__start
      +inputBox(labelEnd, 'xs')
        .date-dropdown
          input(type='text' readonly placeholder = placeholder).date-dropdown__display.input-box__input.date-dropdown__end
