mixin radioGroup
  .input-box__radio-group
    block

mixin radioItem(name, value, label, id)
  .input-box__radio-item
    input(type="radio" name=name value=value id=id).input-box__radio-btn
    span.input-box__radio-mark
    label(for=id).input-box__radio-label= label