mixin radio(options = {})
  label.radio&attributes(attributes)
    input(
      type="radio"
      name=options.name || ''
      value=options.value || ''
      checked=options.checked || false
    ).radio__input
    span.radio__replacer
    span.radio__label= options.label || ''
