include ~UtilBlocks/head
include ~CommonBlocks/header/header
include ~CommonBlocks/form/form
include ~CommonBlocks/room-card/room-card

+head('Cards')
  body.page.page--layout_cards
    +headerUIKit
    main.main-content--page_cards
      .main-content__column
        +formSearchRoom
        +formRegistration
      .main-content__column
        +formBooking({ number: '888', lux: true })
        +formLogin
      .main-content__column
        .js-datepicker-static
          input(type="hidden")
          input(type="hidden")
        +roomCard(['room1.jpg', 'room2.jpg', 'room3.jpg'], 5, 888, '9 990₽', 145, true).room-card--width_m
        +roomCard(['room2.jpg', 'room3.jpg', 'room4.jpg', 'room5.jpg'], 4, 840, '9 900₽', 65).room-card--width_m
