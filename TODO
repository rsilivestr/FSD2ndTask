learn Markdown
  - add component readmes
    + dropdown

perfect 
  - Colors & Fonts
  ~ Form Elements
  - Cards
  - Headers & Footers

dropdown
  ? save data in JSON to local storage

Current page - Landing



CHANGED
  + set dropdown z-index to 30
  + changed double datepicker on form elements page

CHANGE
  - rename media breakpoints
  - refactor containers
  ? close dropdown on apply
  - move common.blocks to components
  - give similar names to dropdown mixins
  - range dropdown months to lowercase
  ? bundle icons with fontello
  - landing page: center form on small screens @include fw.media('<tablet')

ADDED
  + checkbox-group &attributes
  + minimal padding (.full-width-container) to creative footer
  + margin to --has-subnav navlinks on mobile for centered alignmnent
  + fixed width to static datepicker
  + --ratable modifier to rate-box
  + range dropdown scripts
  + shadow to dp
  + apply_filters button
  + room-card lux option to mixin

ADD
  - room-card dots
  - caption for range slider
  - head mixin
  - mainHeaderRu mixin

FIXED
  + lessened checkbox group height
  + fixed headers collapsing on 'headers & footers' page
  + overlapping headers z-indexes on 'headers & footers' page
  + footer column subscribe heading alignmnent on desktop
  + footer column cursor
  + aligned footer subscribtion title to left on desktop
  + added top-margin to main / wrapper (due to fixed header)
  ~ dropdown shadow not expanding to contents (now slightly overlaps with display)

FIX
  ? check header shadows
  ? filters margins

pub on github pages