learn Markdown
  - add component readmes
    + dropdown

perfect 
  - Colors & Fonts
  ~ Form Elements
  - Cards
  - Headers & Footers

dropdown
  ? save data in JSON to local storage

Current page - Landing

CHANGED

CHANGE
  - rename media breakpoints
  ? close dropdown on apply

ADDED
  + checkbox-group &attributes

ADD
  - add shadow to dp
  - add negative margin to icons in mobile menu links with subnav
  - fixed rating
  - room-card dots
  - caption for range slider
  ? apply filters button
  - range dropdown scripts

FIXED
  + lessened checkbox group height

FIX
  - check header shadows
  - room-card lux
  - rise dropdown z-index
  - add top-margin to main / wrapper (due to fixed header)
  - filters margins
  - align footer subscribtion title to left on desktop

pub on github pages